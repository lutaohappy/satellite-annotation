spring:
  application:
    name: satellite-annotation-backend

  profiles:
    active: dev

  # 数据库配置
  datasource:
    url: jdbc:postgresql://192.168.200.77:5432/satellite_annotation?ssl=false&sslfactory=org.postgresql.ssl.NonValidatingFactory
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 60000
      validation-timeout: 30000
      login-timeout: 60

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

# 服务器配置
server:
  port: 4000

# JWT 配置
jwt:
  secret: satellite-annotation-system-secret-key-must-be-changed-in-production
  expiration: 86400000  # 24 小时

# 影像配置
image:
  upload-dir: /Users/taolu/Downloads/test/satellite-annotation/backend/uploads/images/
  tile-dir: /Users/taolu/Downloads/test/satellite-annotation/backend/uploads/tiles/
  max-size: 1073741824  # 1GB

# 日志配置
logging:
  level:
    root: INFO
    com.annotation: DEBUG
    org.springframework.security: WARN
    org.springframework.web.servlet: DEBUG

---
spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:postgresql://192.168.200.77:5432/satellite_annotation
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

jwt:
  secret: ${JWT_SECRET:satellite-annotation-system-secret-key-must-be-changed-in-production}

image:
  upload-dir: /opt/uploads/images/
  tile-dir: /opt/uploads/tiles/

server:
  port: 4000
  address: 0.0.0.0

logging:
  level:
    root: INFO
    com.annotation: DEBUG
    org.springframework.security: DEBUG
